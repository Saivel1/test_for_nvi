# 📹 Video Management API Service

![Python](https://img.shields.io/badge/python-3.13-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109.0-009688.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-336791.svg)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED.svg)

Асинхронный REST API сервис для управления метаданными видеофайлов. Проект выполнен в рамках тестового задания.

Сервис предоставляет возможности создания, фильтрации и обновления статусов видеозаписей, используя современный стек технологий.

---

## 🛠 Технологический стек

* **Язык:** Python 3.13
* **Фреймворк:** FastAPI (Async)
* **База данных:** PostgreSQL 16
* **ORM:** SQLAlchemy 2.0 (Async) + Pydantic v2
* **Пакетный менеджер:** [uv](https://github.com/astral-sh/uv) (современная замена pip/poetry)
* **Контейнеризация:** Docker & Docker Compose

---

## 🚀 Запуск проекта

Предварительно убедитесь, что у вас установлены **Docker** и **Docker Compose**.

### Вариант 1: Быстрый запуск (рекомендуется)
Проект содержит скрипт автоматизации `deploy.sh`, который сам настроит окружение, соберет контейнеры и запустит их.

```bash
# 1. Дайте права на исполнение скрипта
chmod +x deploy.sh

# 2. Запустите скрипт
./deploy.sh

Вариант 2: Ручной запуск

Если вы предпочитаете контролировать процесс вручную:
Bash

# 1. Создайте файл окружения из примера
cp .env.example .env

# 2. Соберите и запустите контейнеры в фоновом режиме
docker compose up --build -d

# 3. (Опционально) Просмотр логов
docker compose logs -f
```

📖 Документация API

После запуска сервиса интерактивная документация доступна по адресам:

    Swagger UI: http://localhost:8000/docs — тестирование запросов прямо из браузера.

    ReDoc: http://localhost:8000/redoc — альтернативная версия документации.

Основные эндпоинты:

    POST /videos — Добавить новое видео.

    GET /videos — Получить список видео (с фильтрацией по статусу, датам, камерам).

    GET /videos/{id} — Получить детальную информацию.

    PATCH /videos/{id}/status — Обновить статус видео.

📂 Структура проекта
```plaintext
.
├── api/                # Эндпоинты (Routers)
├── db/                 # Конфигурация БД и ORM модели
├── schemas/            # Pydantic схемы (валидация данных)
├── tests/              # Тесты
├── .env.example        # Пример переменных окружения
├── deploy.sh           # Скрипт автоматического деплоя
├── docker-compose.yml  # Оркестрация контейнеров
├── Dockerfile          # Сборка образа приложения
├── main.py             # Точка входа приложения
├── pyproject.toml      # Зависимости (uv)
└── uv.lock             # Lock-файл зависимостей
```

⚙️ Переменные окружения

Настройки хранятся в файле .env. Пример содержимого (.env.example):

| Переменная | Описание | Дефолт |
| :--- | :--- | :--- |
| `DB_USER` | Пользователь базы | `test` |
| `DB_PASSWORD` | Пароль БД | `postgres` |
| `DB_NAME` | Имя базы данных | `test` |
| `DB_PORT` | Порт подключения | `5432` |
| `DB_HOST` | Хост БД (имя сервиса в Docker) | `postgres` |